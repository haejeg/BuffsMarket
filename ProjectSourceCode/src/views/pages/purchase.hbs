<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Purchase</title>
    <style>
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f5f5f5;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .listing-image {
            width: 100%;
            max-height: 400px;
            object-fit: cover;
            border-radius: 8px;
        }
        .listing-title {
            font-size: 2em;
            font-weight: bold;
            color: #333;
            margin: 20px 0 10px;
        }
        .listing-price {
            font-size: 1.8em;
            color: #CFB87C;
            margin-bottom: 10px;
        }
        .listing-description {
            font-size: 1.2em;
            color: #555;
            margin-bottom: 20px;
        }
        .custom-btn {
            background-color: #CFB87C;
            color: #fff;
            padding: 10px 20px;
            font-size: 1.2em;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .custom-btn:hover {
            background-color: #fff9d7;
            color: #363636;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Confirm Your Purchase</h1>

        <!-- Listing Details -->
        <img src="{{listing.image_url}}" class="listing-image" alt="Product Image">
        <h2 class="listing-title">{{listing.title}}</h2>
        <p class="listing-price">${{listing.price}}</p>
        <p class="listing-description">{{listing.description}}</p>

        <!-- Purchase Button Form -->
        <form action="/complete-purchase" method="POST">
            <input type="hidden" name="product_id" value="{{listing.id}}">
            <input type="hidden" name="price" value="{{listing.price}}">
            <button type="submit" class="custom-btn">Confirm Purchase</button>
        </form>
    </div>

</body>
</html>
